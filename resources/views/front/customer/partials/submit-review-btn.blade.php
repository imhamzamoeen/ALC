<button class="btn btn-outline-primary py-2 review-trial-action action-btn"
    data-trialclassid="{{ @$student->trialRequest->trialClass->id }}" data-studentid="{{ @$student->id }}">
    {{ __('Review Trial') }}
</button>
